version: "3.8"

services:
  mongodb:
    image: mongo:latest
    container_name: sijawi-mongodb
    ports:
      - "27017:27017"
    volumes:
      - mongodb_data:/data/db
    environment:
      - MONGO_INITDB_DATABASE=wtms
    restart: unless-stopped
    networks:
      - wtms-network

  web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: sijawi-web
    ports:
      - "3000:3000"
    environment:
      - MONGODB_URI=mongodb://mongodb:27017/wtms
      - NODE_ENV=production
    depends_on:
      - mongodb
    restart: unless-stopped
    networks:
      - wtms-network

volumes:
  mongodb_data:
    driver: local

networks:
  wtms-network:
    driver: bridge